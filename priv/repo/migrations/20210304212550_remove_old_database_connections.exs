defmodule Console.Repo.Migrations.RemoveOldDatabaseConnections do
  use Ecto.Migration

  def change do
    # drop table(:channels_labels)
    #
    # alter table(:labels) do
    #   remove :function_id
    # end
  end
end
